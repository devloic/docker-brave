services:
  docker-brave:
    image: localhost/devloic/docker-brave
    privileged: true
    ports:
      - "5800:5800"
      - "5900:5900"
    volumes:
      - "./sharedwithhost:/config/sharedwithhost"
    environment:
      - WEB_AUDIO=1
